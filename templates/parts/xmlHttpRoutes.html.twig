<script>
    // global
    const update_transactions = "{{ path('update') }}";
    // dashboard
    const dashboard_get_monthly_remaining = "{{ path('dashboard_get_monthly_remaining') }}";
    const dashboard_get_account_progress = "{{ path('dashboard_get_account_progress') }}";
    const dashboard_get_month_overview = "{{ path('dashboard_get_month_overview') }}";
    // grid
    const get_category_data = "{{ path('get_category_data') }}";
    const get_grid_data = "{{ path('get_grid_data') }}";
    const split_transaction_get = "{{ path('split_transaction_get') }}";
    const update_category = "{{ path('update_category') }}";
    // secondary account
    const get_secondary_account_data = "{{ path('secondary_controller_data') }}";
    const get_secondary_subaccounts = "{{ path('secondary_controller_get_subaccounts') }}";
    // trees
    const get_tree_data_full_year = "{{ path('get_tree_data_full_year') }}";
    const get_columns_full_year = "{{ path('get_columns_full_year') }}";
    const get_tree_data_last_half_year = "{{ path('get_tree_data_last_half_year') }}";
    const get_columns_last_half_year = "{{ path('get_columns_last_half_year') }}";
    const tree_get_detail_transactions = "{{ path('tree_get_detail_transactions') }}";
    // transfer
    const transfer_index = "{{ path('transfer_index') }}";
    const transfer_tan = "{{ path('transfer_tan') }}";
    const transfer_autocomplete = "{{ path('transfer_autocomplete') }}";
    const transfer_data = "{{ path('transfer_data') }}";
    // settings category group
    const settings_category_group_data = "{{ path('settings_category_group_data') }}";
    const settings_category_group_edit = "{{ path('settings_category_group_edit') }}";
    // settings category
    const settings_category_data = "{{ path('settings_category_data') }}";
    const settings_category_edit = "{{ path('settings_category_edit') }}";
    // settings assignment rules
    const settings_assignment_data = "{{ path('settings_assignment_data') }}";
    const settings_assignment_edit = "{{ path('settings_assignment_edit') }}";
    const settings_assignment_delete = "{{ path('settings_assignment_delete') }}";
    // chart
    const chart_data = "{{ path('chart_data') }}";
    // payal
    const paypal_data = "{{ path('paypal_data') }}";
    const paypal_import = "{{ path('paypal_import') }}";
    const paypal_get_transaction_data = "{{ path('paypal_get_transaction_data') }}";
    const paypal_assign_transaction = "{{ path('paypal_assign_transaction') }}";
</script>